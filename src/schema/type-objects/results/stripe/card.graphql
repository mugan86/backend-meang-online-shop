type ResultStripeCard implements Result {
    status: Boolean!
    message: String!
    token: String!
    fingerprint: String!
    card: ID
    cards: [StripeCard]
}

type StripeCard {
    id: ID!
    fingerprint: String
}