language: node_js
node_js:
  - "14"
install:
  - npm install
script:
  - npm run build
  - npm test

# Configuración para CD en Heroku
deploy:
  provider: heroku
  api_key:
    secure: "$HEROKU"
  on:
    all_branches: true