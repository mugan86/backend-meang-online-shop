language: node_js
node_js:
  - "14"
before_install:
  - rvm install 2.7.5
install:
  - npm install
script:
  - npm run build
  - npm test
  - gem install faraday -v 1.9.3

# Configuración para CD en Heroku
deploy:
  provider: heroku
  api_key:
    api-2021: $HEROKU
  app:
    api-2021: gamezonia